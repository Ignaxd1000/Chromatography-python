[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "chromatography-python"
version = "0.1.0"
description = "Chromatography CLI utilities"
requires-python = ">=3.10"

[project.scripts]
chroma = "main:main"

[tool.setuptools]
py-modules = ["main"]
